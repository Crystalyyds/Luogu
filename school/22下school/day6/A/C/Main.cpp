#include <bits/stdc++.h>
using namespace std;
string a, b, c;
list<char> sa, sb;
int arr[26];
int main() {
  cin >> a >> b;
  for (int i = 0; i < a.size(); i++) {
    sa.push_back(a[i]);
  }
  for (int i = 0; i < b.size(); i++) {
    sb.push_back(b[i]);
  }
  int x = 0;
  int y = 0;
  for (int i = 0; i < a.size(); i++) {
    int temp = b.find(a[i]);
    if (temp =) }

  return 0;
}