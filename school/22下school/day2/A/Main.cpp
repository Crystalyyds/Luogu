#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, array[1000];
  scanf("%d", &n);
  for (int i = 1; i <= n; i++) {
    int w;
    scanf("%d", &w);
    for (int j = 1; j <= w; w++) {
      cin >> array[i];
    }
  }
}