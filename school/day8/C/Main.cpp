#include <bits/stdc++.h>
using namespace std;
int t, arr[100], brr[100];
int main()
{
    scanf("%d", &t);
    for (int i = 1; i <= t; i++)
    {
        cin >> arr[i] >> brr[i];
    }
    return 0;
}