#include <bits/stdc++.h>
using namespace std;

int main()
{
    double t;
    int n = 4, z = 125;
    cin >> t;
    cout << int(n * z / 100.0) << " " << (n * z) / 100.0 << endl;
    cout << (int(n * z / 100.0) == (n * z / 100.0)) << endl;
}