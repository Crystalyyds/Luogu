#include <bits/stdc++.h>
using namespace std;
int n;
int main()
{
    cin >> n;
    while (n--)
    {
        int m;
        cin >> m;
        long long arr[110];
        for (int i = 1; i <= m; i++)
        {
            cin >> arr[i];
        }
        if (m % 2 == 0)
            ;
    }
}