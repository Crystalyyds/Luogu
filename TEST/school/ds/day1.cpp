#include <bits/stdc++.h>
using namespace std;
const int N = 3e3 + 10;
int op;
int arr[N][N];
bool st[N];
int n, t;

int main()
{
    cin >> op;
    while (op--)
    {
        memset(arr, 0, sizeof arr);
        memset(st, false, sizeof st);
        cin >> n;
        for (int i = 1; i <= n; i++)
        {
            cin >> arr[i];
        }
        for (int i = 1; i <= n; i++)
        {
            for (int j = 1; j <= n; j++)
            {
                if ()
            }
        }
    }
}